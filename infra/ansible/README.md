# Ansible

Playbooks that configure the Ubuntu VPS: hardening (SSH keys, UFW, Fail2Ban, unattended upgrades), Docker, k3s, Helm.

**Status: placeholder — populated in Sprint 7** (see [docs/roadmap.md](../../docs/roadmap.md)).
