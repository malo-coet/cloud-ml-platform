# Kubernetes manifests

Manifests for deploying the platform on k3s (Deployment, Service, ConfigMap, Secret, Ingress and volumes per component).

**Status: placeholder — populated in Sprint 7** (see [docs/roadmap.md](../../docs/roadmap.md)).
